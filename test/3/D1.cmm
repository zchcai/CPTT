int changeFormat(int r1, int r2)
{
	int r3;
	r1 = r1 * 3 + 239 - 244 + 247 - r2 * 2;
	r3 = r2 * 2 + r1 * 3 + 12 - 239;
	r2 = r3 * 2 - r1 * 1 * (1 + 1);
	return r2 + r3;
}

int main()
{
	int x = 1 + 3 + 80 * 2 - 11, y = 9 * 13 / 6 + 2 - 11 + 12 * 5, z = 3 * (22 + 1) -2;
	int a = x + y;
	int b = x + y + z;
	int c = a + b;
	int d = x + y + a + b + c;
	int i = 5 - 5 + 10 * 1 - 10;
	i = i + 1 + 0;
	i = i + 3 + 1;
	i = i - 2 - 2;
	if (a + b > x + y){
		d = y + 1 + a + b;
		while (d + 1 > i + 1 - 2 * i + 3 * i){
			a = changeFormat(c, d);
			i = i + d + 1 - d;
		}
		write(a);
		while(a > 200){
			i = a * 20 + 90;
			a = a - x + 5 - a * 0 - 5;
			i = a * 20 + 1 + changeFormat(a, b);

		}
		write(i);
		a = a + b;
		b = a + b;
		c = a + b;
	}
	return 0;
}
